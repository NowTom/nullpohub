- if user_signed_in?
  %span.muted
    %i.icon-user
    = current_user.name
  = link_to 'Logout', '/logout', method: :delete, class: 'btn btn-danger'
- else
  = link_to 'Login', user_omniauth_authorize_path(:github), class: 'btn btn-primary'
